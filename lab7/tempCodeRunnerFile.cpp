 if (total > highestTotal) {
            highestTotal = total;
            highestIndex = i;
        }
        if (total < lowestTotal) {
            lowestTotal = total;
            lowestIndex = i;
        }